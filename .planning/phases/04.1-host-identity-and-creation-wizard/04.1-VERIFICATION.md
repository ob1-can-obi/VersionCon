---
phase: 04.1-host-identity-and-creation-wizard
verified: 2026-05-09T10:25:00Z
status: human_needed
score: 4/4 must-haves verified (code-level); SC-1 wizard webview interaction requires human UAT
overrides_applied: 0
human_verification:
  - test: "Wizard step 1 pre-fills displayName with default chain"
    expected: "Open Extension Development Host, run `versioncon.hostSession`. Wizard step 1 shows TWO inputs (Session Name + Your Display Name). The Display Name input is pre-populated with: (a) any prior `versioncon.displayName` workspace setting, else (b) `git config user.name` (if a git repo + git available), else (c) `os.userInfo().username`, else (d) the literal 'Host'. The Next button is disabled until both fields are non-empty after trim."
    why_human: "Webview render + DOM-side input pre-fill behavior requires a live `vscode.window.createWebviewPanel` instance and message-channel handling — both unavailable in the unit-test runner. Source-grep test (Test 1 in `Phase 4.1 cross-cutting regression`) verifies the underlying source-level contract (resolveDefaultDisplayName chain present) but cannot exercise the live webview."

  - test: "Wizard step 1 persists chosen displayName to workspace settings"
    expected: "After typing a displayName (e.g. 'Alice') and clicking Next, open `.vscode/settings.json` in the workspace — the file contains `\"versioncon.displayName\": \"Alice\"`. Re-running `versioncon.hostSession` immediately after closing the wizard shows the displayName input pre-filled with 'Alice'."
    why_human: "Requires real `vscode.workspace.getConfiguration().update(... ConfigurationTarget.Workspace)` which writes to the live workspace's `.vscode/settings.json`. Source-grep test asserts `ConfigurationTarget.Workspace` is referenced; the disk write itself is environment-dependent."

  - test: "Wizard step 1 rejects invalid displayName (empty / >64 chars / control chars)"
    expected: "Type empty value → Next disabled. Type 65+ chars → on Next click, error 'Display name must be 64 characters or fewer.' renders without advancing the step. Paste a control character (e.g. \\x07 BEL) into the input → on Next click, error 'Display name cannot contain control characters.' renders without advancing. The wizard does NOT advance to step 2 in any of these cases."
    why_human: "Validation is enforced in `handleWizardNext`, but the user-facing error toast and step-non-advance behavior requires the live webview round-trip. Source-grep test asserts the three error literals are present in `WizardPanel.ts`; runtime UX verification needs human testing."
---

# Phase 4.1: Host Identity + Creation Wizard — Verification Report

**Phase Goal:** Two architectural fixes to v1 session creation surfaced during Phase 4 multi-window UAT (2026-05-08): (Defect A) the Create Session wizard must explicitly prompt the host for their display name with sensible defaults (git config user.name → OS username → 'Host'), validation, and persistence; (Defect B) the session creator must be the host by construction, not by "first WebSocket auth wins" race.
**Verified:** 2026-05-09T10:25:00Z
**Status:** HUMAN_NEEDED — all 4 ROADMAP Success Criteria VERIFIED at the code level; wizard webview interaction (SC-1 user-facing) deferred to multi-window UAT per Plan 04-11 precedent (UI-SPEC literal verification via source-grep tests).
**Re-verification:** No — initial verification.

---

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP Success Criterion) | Status | Evidence |
|---|------------------------------------|--------|----------|
| 1 | Creating a session opens a wizard step that prompts the user for their display name. Default chain: git `user.name` → OS username → 'Host'. The chosen value flows into SessionHost and is reflected in MEMBERS row, chat-message envelopes, and system-event bodies. | VERIFIED (code) / UAT pending | `WizardPanel.resolveDefaultDisplayName` at `src/ui/WizardPanel.ts:86` implements the documented 4-step chain (settings → git → os → 'Host'). `wizard.js:84-87` renders the Display Name input. `WizardPanel.ts:541-546` allocates `HostIdentity { memberId, displayName, hostAuthSecret }` and passes it to `new SessionHost(config, hostIdentity)`. `SessionHost.constructor` (line 133-140) consumes `hostIdentity.displayName` into `this.hostDisplayName`. Body interpolation: `broadcastBranchCreated` (line 822) uses `members.get(branch.createdBy)?.member.displayName ?? this.hostDisplayName`. Source-grep Test 1 in `Phase 4.1 cross-cutting regression` pins the contract. |
| 2 | When ANY remote member authenticates over WebSocket — regardless of timing — they are assigned role:'member', never role:'host'. The session creator's pre-set hostMemberId is bound BEFORE the WebSocket listener accepts connections; the "first authenticated wins" branch in SessionHost.ts is removed. | VERIFIED | `SessionHost.constructor` at line 136 sets `this.hostMemberId = hostIdentity.memberId` synchronously before `start()` can bind. `handleAuthRequest` at lines 537-556 defaults `role = 'member'` and only upgrades to 'host' on `crypto.timingSafeEqual(claimedSecret, this.hostAuthSecret)` after a length pre-check + typeof guard. The pre-existing "first authenticated member is the host" branch is gone (grep returns 0). Test 2 in `Phase 4.1 cross-cutting regression` boots host + connects remote FIRST + asserts `role: 'member'`. Tests in `Phase 4.1 host pre-registration` cover happy path, wrong secret, wrong-length secret, and admin-gate continuity. |
| 3 | Reverts of remote members' pushes still attribute the action to the original pusher (`record.memberDisplayName`), preserving the CR-01-NEW closure from plan 04-15. | VERIFIED | `broadcastRevert` at `SessionHost.ts:772-798` interpolates body as `${record.memberDisplayName} reverted ${fileCount} file(s)` and threads `record.memberId` / `record.memberDisplayName` into `appendAndBroadcastSystemEvent`. CR-01-NEW grep `hostDisplayName} reverted` returns 0; `record.memberDisplayName} reverted` is present. Test 4 in cross-cutting regression suite asserts the same under HostIdentity model. |
| 4 | All existing 317 Phase 4 tests still pass without modification. New tests cover wizard contract, race protection, and remote-auth-never-gets-host. | VERIFIED | `npm test` reports **337 passing** (was 317 baseline pre-phase). Net delta +20 = +3 (04.1-01 protocol round-trip) + +6 (04.1-02 host pre-registration) + +11 (04.1-04 cross-cutting regression). All 8 pre-existing Phase 4 suites preserved at baseline counts (Phase 4 host relay, host input validation, system events in chat, system event correctness, chat panel lifecycle). `tsc --noEmit` exits 0. |

**Score: 4/4 ROADMAP Success Criteria VERIFIED at the code level.**

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/types/session.ts` | `export interface HostIdentity { memberId: string; displayName: string; hostAuthSecret: string }` with security JSDoc | VERIFIED | Lines 48-52; JSDoc lines 25-47 documents the loopback-only secret-flow boundary. |
| `src/network/protocol.ts` | `AuthRequest` extended with optional `hostAuthSecret?: string`; permissive parse contract preserved | VERIFIED | Line 69 declares the field; lines 41-68 JSDoc the host-loopback-only invariant. `parseMessage`, `sendMessage`, `MessageType`, `ProtocolMessage` discriminated union all preserved (greps return 1 each). |
| `src/host/SessionHost.ts` | Constructor accepts `HostIdentity`; `hostMemberId` set in constructor body; `handleAuthRequest` gates role:'host' on `crypto.timingSafeEqual` of `hostAuthSecret` | VERIFIED | `import type { HostIdentity }` at line 13. Constructor line 133, fields at lines 55, 66, 74. handleAuthRequest secret check at lines 537-556. Old "first authenticated" branch removed (grep = 0). `removeMember` reset preserved at line 1220-1222. |
| `src/ui/WizardPanel.ts` | `resolveDefaultDisplayName` 4-step chain; `handleWizardNext` validation (non-empty / ≤64 / no control chars); workspace persistence; `HostIdentity` allocation in `handleWizardComplete`; widened `onSessionStartedCallback` signature | VERIFIED | `resolveDefaultDisplayName` at line 86 (settings → git execFileSync timeout 1000ms → os.userInfo() → 'Host'). Validation at lines 405-429 (3 distinct error messages). Persistence at lines 437-446 (ConfigurationTarget.Workspace + non-fatal error swallow). HostIdentity allocation at lines 541-546. Callback signature `(host, sessionName, hostIdentity: HostIdentity)` at line 134. |
| `src/ui/webview/wizard/wizard.js` | Step 1 renders TWO inputs (sessionName + displayName); Next disabled until both non-empty after trim; `wizard-next` payload widened to `{sessionName, displayName}` | VERIFIED | `renderStep1` at lines 69-94 emits `<input id="display-name" maxlength="64">`. `attachListeners` at lines 154-173 wires `updateNextDisabled` to BOTH inputs. `btnNext` step 1 click handler at lines 178-187 posts both fields. |
| `package.json` | `contributes.configuration.versioncon.displayName` declaration | VERIFIED | Lines 291-298 declare `configuration.title: "VersionCon"` + `properties["versioncon.displayName"]` (type string, default '', description present). |
| `src/extension.ts` | `HostIdentity` import; `let activeHostIdentity: HostIdentity \| null = null` at module scope; `wireHostEvents` accepts `hostIdentity` and stores it; cleared on session-ended; `hostMemberId = 'local-user'` placeholder UNCHANGED (IIFE admin-bypass invariant) | VERIFIED | Line 14 import. Line 70 declaration. `wireHostEvents` widened signature at line 595. `activeHostIdentity = hostIdentity` assignment at line 602. `activeHostIdentity = null` clear at line 669. `let hostMemberId = 'local-user'` at line 49 UNCHANGED (Test 11 pin). IIFE bypass gates at lines 1315, 1720, 1801 unchanged. |
| `src/test/suite/protocol.test.ts` | `AuthRequest hostAuthSecret round-trip (Phase 4.1)` suite — 3 tests | VERIFIED | Suite at line 208; tests at lines 209, 227, 247 (backwards-compat / forward-compat / tolerant-parse). All 3 passing per `npm test`. |
| `src/test/suite/host.test.ts` | `Phase 4.1 host pre-registration` (6 tests) + `Phase 4.1 cross-cutting regression` (11 tests) suites; `makeHostIdentity` helper; 5 call-site updates | VERIFIED | `makeHostIdentity` at line 82. 5 call sites use `makeHostIdentity(HOST_NAME)` (lines 208, 671, 999, 1362, 1491). `Phase 4.1 host pre-registration` suite at line 1794 (6 tests, lines 1816, 1860, 1871, 1915, 1932, 1980). `Phase 4.1 cross-cutting regression` suite at line 2030 (11 tests, lines 2037, 2078, 2106, 2153, 2190, 2238, 2279, 2334, 2378, 2424, 2484). All 17 passing per `npm test`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `src/ui/WizardPanel.ts` (handleWizardComplete:546) | `src/host/SessionHost.ts` (constructor:133) | `new SessionHost(config, hostIdentity)` — second arg is HostIdentity | WIRED | grep returns 1; old `new SessionHost(config, hostDisplayName)` form: 0. tsc --noEmit exits 0 — call site type-checks against the widened constructor. |
| `src/ui/WizardPanel.ts` (handleWizardComplete:576) | `src/extension.ts` (wireHostEvents:595) | `onSessionStartedCallback(host, sessionName, hostIdentity)` — three-arg callback | WIRED | extension.ts callback registration at line 961-962 forwards the triple. activeHostIdentity stored at line 602; cleared at line 669. |
| `src/host/SessionHost.ts` (handleAuthRequest:538) | `src/network/protocol.ts` (AuthRequest.hostAuthSecret) | `msg.hostAuthSecret` read after invite-code validation; gated by `typeof === 'string'` + length pre-check + `crypto.timingSafeEqual` | WIRED | Field declaration at protocol.ts:69; consumer at SessionHost.ts:538-555. Round-trip test (protocol.test.ts:227) verifies wire-shape. Length pre-check at line 541. Constant-time compare at line 547. |
| `src/host/SessionHost.ts` (this.hostMemberId) | `src/host/SessionHost.ts` (admin gates at handleKickRequest:625, handleRegenerateInvite:647) | `requesterId !== this.hostMemberId` check; hostMemberId is non-null from construction (line 136) and re-pointed to ws-authed memberId on successful host auth (line 554) | WIRED | Pre-allocated UUID is the construction value; re-point to ws-authed id on host auth preserves admin-gate semantics. Test in `Phase 4.1 host pre-registration` (line 1932) asserts kickMember works after loopback host auth. |
| `src/extension.ts` (line 49: `let hostMemberId = 'local-user'`) | `src/extension.ts` (IIFE admin-bypass gates at lines 1315, 1720, 1801) | `currentMemberId !== hostMemberId` — both sides remain `'local-user'` for the host-bypass to resolve to false | WIRED | `let hostMemberId = 'local-user'` UNCHANGED (Test 11 source-grep pins it). The wire-side host identity (UUID on `SessionHost.this.hostMemberId`) is intentionally decoupled from the IIFE-bypass placeholder; documented at extension.ts:62-69. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|----|
| `wizard.js` step 1 displayName input | `state.displayName` | Initial value from `WizardPanel.resolveDefaultDisplayName()` (settings/git/os/'Host'); subsequent edits via DOM input → `wizard-next` payload | Yes (real user input + 4-step default chain with execFileSync to git) | FLOWING |
| `WizardPanel.handleWizardComplete` hostIdentity | hostIdentity local const | `crypto.randomUUID()` × 2 + `this.state.displayName` | Yes (real UUIDs + user-entered displayName) | FLOWING |
| `SessionHost.this.hostDisplayName` | private readonly field | `hostIdentity.displayName` from constructor (line 135) | Yes (sourced from real wizard input) | FLOWING |
| `SessionHost.this.hostAuthSecret` | private readonly field | `hostIdentity.hostAuthSecret` from constructor (line 137) | Yes (real UUID) | FLOWING |
| `extension.ts activeHostIdentity` | module-level let | `wireHostEvents` arg from `WizardPanel.createOrShow` callback (line 962) | Yes (real triple from wizard) | FLOWING |

All renderable data is sourced from real producers — no hardcoded empty arrays, no static fallbacks at the call site. The wizard's `state.displayName` is initialized to a real default-chain result; the `hostIdentity` triple is materialized only when the user commits to creating a session (minimizes secret lifetime).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TypeScript compiles cleanly | `npx tsc --noEmit` | exit 0 | PASS |
| Full test suite passes | `npm test` | 337 passing, 0 failing, 66 pending (deliberate) | PASS |
| `Phase 4.1 host pre-registration` suite | `npm test` (filtered to phase) | 6 passing | PASS |
| `Phase 4.1 cross-cutting regression` suite | `npm test` (filtered to phase) | 11 passing | PASS |
| `AuthRequest hostAuthSecret round-trip (Phase 4.1)` suite | `npm test` (filtered to phase) | 3 passing | PASS |
| Phase 4 closure greps preserved (CR-01-NEW × 4, CR-02-NEW, CR-03-NEW × 2, 04-13 kind:'user') | grep cascade | All 8 grep counts match expected baseline | PASS |
| Defect B "first authenticated" branch removed | `grep -c "first authenticated member is the host" src/host/SessionHost.ts` | 0 | PASS |
| Defect A `resolveDefaultDisplayName` present | `grep -c "resolveDefaultDisplayName" src/ui/WizardPanel.ts` | 2 (declaration + call site) | PASS |
| IIFE admin-bypass invariant pinned | `grep -c "let hostMemberId = 'local-user'" src/extension.ts` | 1 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| NET-01 | 04.1-03-PLAN.md (declared in `requirements:`) | Host can create a LAN session with full control (port, bandwidth limits, **credentials per user**) | SATISFIED (refines Phase 1) | The "credentials per user" facet is the displayName the host now explicitly chooses (Plan 04.1-03's wizard step 1 input, default chain, validation, persistence). Combined with the Phase 1 wizard's port/bandwidth/invite-code controls, NET-01's "full control" is now complete for the host's identity surface. |
| NET-02 | 04.1-03-PLAN.md (declared in `requirements:`) | Host setup uses a step-by-step wizard (sliding window style) with only essential fields | SATISFIED (refines Phase 1) | Wizard step 1 widens to TWO inputs (sessionName + displayName) — both are upfront-identity essentials. Steps 2-4 (network, credentials, share) preserved verbatim. The 4-step sliding-window pattern is intact; displayName is the only addition, and it is essential (without it, the host appears as the literal 'Host'). |

REQUIREMENTS.md tracker line 131 lists NET-01 as `Phase 1 — In Progress (01-02)`; Phase 4.1 advances this to a more complete state for the identity surface. Line 132 lists NET-02 as `Phase 1 — Pending` and Phase 4.1's wizard step refines it. No orphaned requirements — Plan 04.1-01/02/04 declare `requirements: []` (refinement plans, not new requirement coverage), and Plan 04.1-03's `requirements: [NET-01, NET-02]` is the sole declaration matching the phase's ROADMAP entry.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | All anti-pattern greps return clean. No TODOs/FIXMEs/placeholders/stub returns introduced in this phase's modifications. The `let hostMemberId = 'local-user'` placeholder in extension.ts is INTENTIONAL (IIFE admin-bypass invariant, pinned by Test 11) — explicitly documented at extension.ts lines 46-69 and is NOT a stub. |

### Human Verification Required

Three multi-window UAT items required to verify the wizard's user-facing webview behavior. Source-grep tests at the codebase level pass for all three; the live-webview interaction is the comprehensive layer per Plan 04-11 precedent (UI-SPEC literal verification via source-grep tests; per STATE.md decisions table).

#### 1. Wizard step 1 pre-fills displayName with default chain

**Test:** Open Extension Development Host, run `versioncon.hostSession`. Wizard step 1 shows TWO inputs (Session Name + Your Display Name). Display Name input pre-populated with: (a) any prior `versioncon.displayName` workspace setting, else (b) `git config user.name` (if a git repo + git available), else (c) `os.userInfo().username`, else (d) the literal 'Host'. The Next button is disabled until both fields are non-empty after trim.
**Expected:** Display Name input pre-fills with the highest-priority available value from the 4-step chain. Next disabled when either input is empty.
**Why human:** Webview render + DOM-side input pre-fill behavior requires a live `vscode.window.createWebviewPanel` instance and message-channel handling — both unavailable in the unit-test runner.

#### 2. Wizard step 1 persists chosen displayName to workspace settings

**Test:** Type a displayName (e.g. 'Alice') in step 1, click Next. Open `.vscode/settings.json` in the workspace.
**Expected:** File contains `"versioncon.displayName": "Alice"`. Re-running `versioncon.hostSession` immediately shows the displayName input pre-filled with 'Alice'.
**Why human:** Requires real `vscode.workspace.getConfiguration().update(... ConfigurationTarget.Workspace)` write to the live workspace's `.vscode/settings.json`. Source-grep verifies the API call is present; the disk write is environment-dependent.

#### 3. Wizard step 1 rejects invalid displayName (empty / >64 chars / control chars)

**Test:** (a) Empty value → confirm Next is disabled. (b) Type 65+ chars → click Next → expect error 'Display name must be 64 characters or fewer.' rendered without advancing the step. (c) Paste a control character (e.g. `\x07` BEL) → click Next → expect error 'Display name cannot contain control characters.' without advancing.
**Expected:** Three distinct error messages, none of them advance to step 2.
**Why human:** Validation logic itself is unit-testable (and pinned at the source level by Test 1), but the user-facing error toast and step-non-advance UX requires the live webview round-trip.

### Gaps Summary

No gaps. All four ROADMAP Success Criteria are satisfied at the code level. SUMMARY claims (337 passing tests, +20 net delta, all artifacts present, all closures preserved) verified directly against the codebase via greps + tsc + npm test execution. The phase ships ready for multi-window UAT — three human-verification items document the wizard webview interactions that cannot be exercised in the unit-test runner. Defects A and B are structurally closed:

- **Defect A**: Wizard step 1 explicitly prompts for displayName with the documented default chain (settings → git → os → 'Host'), validates input (non-empty / ≤64 chars / no control chars), and persists to workspace settings. The literal-'Host' fallback is now only reached when ALL four steps fail (empty settings + no git + no os.userInfo).
- **Defect B**: `SessionHost.this.hostMemberId` is set in the constructor body (line 136) BEFORE `start()` binds the WebSocket listener — eliminating the race window. `handleAuthRequest` defaults `role = 'member'` and only upgrades to 'host' on `crypto.timingSafeEqual(claimedSecret, this.hostAuthSecret)` (length-pre-checked, typeof-guarded). Remote clients without the secret always get role:'member' regardless of timing.

All Phase 4 closures (CR-01-NEW, CR-02-NEW, CR-03-NEW, plan 04-13 CR-01) preserved without regression. The IIFE admin-bypass invariant in `extension.ts` is decoupled from the wire-side host identity and explicitly pinned by Test 11.

---

*Verified: 2026-05-09T10:25:00Z*
*Verifier: Claude (gsd-verifier)*
