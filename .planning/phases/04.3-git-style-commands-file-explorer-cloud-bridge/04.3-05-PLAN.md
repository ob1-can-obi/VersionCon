---
phase: 04.3-git-style-commands-file-explorer-cloud-bridge
plan: 05
type: execute
wave: 5
depends_on:
  - 04.3-01
  - 04.3-02
  - 04.3-03
  - 04.3-04
files_modified:
  - README.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - 04.3-SC-2
  - 04.3-SC-7-docs
must_haves:
  truths:
    - "README.md contains a 'Lifecycle Tour' section that walks through create project locally → LAN collab → push to cloud git → next version via pull + fresh session"
    - "README.md contains a 'VersionCon for Git Users' quick-ref table mapping each new VersionCon verb (push, pull, checkout, branch, log, diff, merge, exportToGitRemote, importFromGitRemote) to its git equivalent and to the underlying canonical command id"
    - ".planning/PROJECT.md gains a 'Lifecycle Tour' section reflecting the same flow at the planning-document layer, so future phase planners (Phase 5 / 6 / 7) inherit the lifecycle vocabulary"
    - ".planning/PROJECT.md 'VersionCon for Git Users' table is included so the planning-doc reflects the user-facing vocabulary"
    - "No code or test files are touched in this plan — pure documentation"
  artifacts:
    - path: "README.md"
      provides: "User-facing 'Lifecycle Tour' + 'VersionCon for Git Users' quick-ref"
      contains: "Lifecycle Tour"
    - path: ".planning/PROJECT.md"
      provides: "Planning-doc-side 'Lifecycle Tour' + git-vocabulary table"
      contains: "Lifecycle Tour"
  key_links:
    - from: "README.md Lifecycle Tour"
      to: "VersionCon commands"
      via: "step-by-step walkthrough referencing exact Command Palette entries"
      pattern: "VersionCon: Push|VersionCon: Pull|VersionCon: Export to Git Remote"
    - from: "README.md VersionCon-for-Git-users table"
      to: "Underlying canonical command ids"
      via: "table rows map verb → git equivalent → canonical command id"
      pattern: "versioncon\\.cmd\\.push|versioncon\\.push"
---

<objective>
Document the full lifecycle that Phase 4.3 enables — create project locally → LAN collab via VersionCon → push to cloud git → start v2 via pull + fresh session. Add a "VersionCon for Git Users" quick-ref so users coming from git immediately understand which VersionCon command does what they expect.

Two surfaces:
1. README.md (user-facing) — what a new user reads first
2. .planning/PROJECT.md (planning-doc layer) — what future phase planners inherit

Purpose: Phase 4 UAT user feedback explicitly asked "let it also mimic git so people understand better" — git-vocabulary aliases (Plan 04.3-01) are necessary but not sufficient. Users need a translation guide AND a clear story for the LAN→cloud handoff. This plan closes SPEC SC-2 (Command Palette discoverability + README quick-ref) and SC-7's documentation half (lifecycle).

Output: README.md + PROJECT.md updated. No code changes. Phase 4.3 ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.3-git-style-commands-file-explorer-cloud-bridge/04.3-SPEC.md
@.planning/phases/04.3-git-style-commands-file-explorer-cloud-bridge/04.3-01-PLAN.md
@.planning/phases/04.3-git-style-commands-file-explorer-cloud-bridge/04.3-02-PLAN.md
@.planning/phases/04.3-git-style-commands-file-explorer-cloud-bridge/04.3-03-PLAN.md
@.planning/phases/04.3-git-style-commands-file-explorer-cloud-bridge/04.3-04-PLAN.md
@./CLAUDE.md
@README.md

<interfaces>
<!-- Command-id mapping to document, sourced from prior plans in this phase -->

| Git verb         | VersionCon canonical id              | Git-style alias (Plan 04.3-01) | Behavior summary |
|------------------|---------------------------------------|---------------------------------|------------------|
| git push         | versioncon.push                       | versioncon.cmd.push             | Pushes staged files (drag) OR auto-stages workspace-vs-branch diff (Plan 04.3-02) |
| git pull         | versioncon.pull                       | versioncon.cmd.pull             | Plan 04.3-02: copies branch→workspace with PUSH-11 conflict prompt for byte-conflicts |
| git checkout     | versioncon.switchBranch               | versioncon.cmd.checkout         | Switches active branch |
| git branch       | versioncon.createBranch               | versioncon.cmd.branch           | Creates a new branch from active |
| git log          | versioncon.showPushHistory            | versioncon.cmd.log              | Shows push history modal |
| git diff         | versioncon.diff (Plan 04.3-03)        | versioncon.cmd.diff             | QuickPick over workspace-vs-branch diff; per-file diff is versioncon.previewDiff |
| git merge        | versioncon.mergeBranch                | versioncon.cmd.merge            | Drag-and-drop or structured merge |
| git push (cloud) | versioncon.exportToGitRemote          | (no alias — distinct intent)    | Plan 04.3-04: ships branch to a real Git remote (host-only, admin-gated) |
| git clone        | versioncon.importFromGitRemote        | (no alias)                      | Plan 04.3-04: clones a real Git remote into a fresh VersionCon branch (host-only) |
| git sync/fetch   | versioncon.sync                       | (no alias)                      | Existing PUSH-10/11 — resolves SyncTracker out-of-sync set after teammate push |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'Lifecycle Tour' + 'VersionCon for Git Users' to README.md</name>
  <files>README.md</files>
  <action>
1. Read the current `README.md` to find the existing structure. If README.md does not yet exist or is a stub, create it with a top-level structure: title, one-paragraph elevator pitch, install/launch instructions, then the two new sections below. If README.md exists, preserve all existing content and INSERT the two new sections — place them after any existing "Features" / "Installation" section and before any "Development" / "Contributing" section. Do not reorder existing sections.

2. Append (or insert at the chosen location) the following sections verbatim, customizing only the placeholder bits in `{braces}`:

   ```markdown
   ## Lifecycle Tour

   VersionCon takes a project from "Alice creates a folder" through "we shipped v1 to GitHub and started v2" without ever leaving VS Code. Here is the full lifecycle.

   ### 1. Create the project locally
   Alice opens VS Code on an empty folder named `PROJECT/` and runs **VersionCon: Host Session** from the Command Palette. The host setup wizard walks her through display name, network configuration, and an invite code. When she finishes, VersionCon is listening on the LAN and `.versioncon/branches/main/` is initialized.

   The `.versioncon/` directory is hidden from her File Explorer automatically (Phase 4.3) — she sees only her project files.

   ### 2. Teammates join over LAN
   Bob runs **VersionCon: Join Session**, enters Alice's host IP and the invite code. Within seconds he is in. His `.versioncon/` directory mirrors Alice's; both can see "Branch Files" (the shared branch view) and "My Workspace" (their personal scratch pad) in the VersionCon sidebar.

   ### 3. Edit and push (no drag required)
   Either teammate edits files in VS Code's native File Explorer like they would for any project. When the workspace has unsaved-to-branch changes, the status bar shows `$(git-pull-request) N local changes`.

   Clicking the indicator runs **VersionCon: Diff** — a QuickPick preview of every file that would be pushed.

   Running **VersionCon: Push** (or any of its aliases — `versioncon.cmd.push`) diffs the workspace against the branch dir, then prompts for a message and pushes. The push broadcasts to teammates and appears in their Activity log within 2 seconds.

   Drag-and-drop staging via the split-pane is still available for power-users — both flows coexist.

   ### 4. Pull a teammate's push
   When Bob's push arrives, Alice sees a notification (soft, non-blocking — Phase 4). She runs **VersionCon: Pull** which copies the branch state into her workspace. If she had unsaved local edits that conflict, she gets the standard "Keep mine / Take branch / Show diff" prompt (PUSH-11) per file.

   ### 5. Ship v1 to a real Git remote
   When the team is done, Alice (host, admin) runs **VersionCon: Export to Git Remote**. She supplies a GitHub URL, a branch name on the remote (defaults to `main`), and a commit message. VersionCon shows a confirmation modal with the URL + file count, then runs `git init` / `git add .` / `git commit -m` / `git remote add origin` / `git push -u origin <branch>` inside `.versioncon/branches/main/`. Output streams to the `VersionCon: Git Bridge` Output channel for full visibility.

   `git log` on the remote now shows the commit. The project is live on GitHub.

   ### 6. Start v2 from the remote
   For the next version, the team starts fresh: Alice runs **VersionCon: Import from Git Remote**, enters the GitHub URL + branch name + a new local VersionCon branch name (e.g. `v2-work`). VersionCon clones the remote into `.versioncon/branches/v2-work/` and registers the branch. The team rejoins the session (or starts a new one) and the loop begins again.

   ## VersionCon for Git Users

   If you already think in git, this table is a translation key. Every git-style command is a Command Palette entry under `VersionCon:`; the underlying canonical command id is shown for scripting.

   | Git verb           | VersionCon command (Command Palette)          | Git-style alias                | Canonical command id              | Notes |
   |--------------------|------------------------------------------------|--------------------------------|-----------------------------------|-------|
   | `git push`         | **VersionCon: Push**                           | `VersionCon: Push` (alias)     | `versioncon.push`                 | Auto-stages workspace diff when nothing is drag-staged |
   | `git pull`         | **VersionCon: Pull**                           | `VersionCon: Pull` (alias)     | `versioncon.pull`                 | Per-file conflict prompt for byte-conflicts (PUSH-11) |
   | `git checkout <b>` | **VersionCon: Switch Branch**                  | `VersionCon: Checkout` (alias) | `versioncon.switchBranch`         | Switches `.versioncon/branches/{active}` symlink |
   | `git branch <new>` | **VersionCon: Create Branch**                  | `VersionCon: Branch` (alias)   | `versioncon.createBranch`         | Copies current branch as starting point |
   | `git log`          | **VersionCon: Push History**                   | `VersionCon: Log` (alias)      | `versioncon.showPushHistory`      | Full revert capability per push |
   | `git diff`         | **VersionCon: Diff**                           | `VersionCon: Diff` (alias)     | `versioncon.diff`                 | QuickPick over changed files; per-file diff is `versioncon.previewDiff` |
   | `git merge <b>`    | **VersionCon: Merge Branch**                   | `VersionCon: Merge` (alias)    | `versioncon.mergeBranch`          | Drag-and-drop or structured walkthrough |
   | `git push <url>`   | **VersionCon: Export to Git Remote**           | (no alias)                     | `versioncon.exportToGitRemote`    | Host-only, admin-gated. Ships branch to a real Git remote. |
   | `git clone <url>`  | **VersionCon: Import from Git Remote**         | (no alias)                     | `versioncon.importFromGitRemote`  | Host-only. Clones a real Git remote into a fresh VersionCon branch. |
   | `git fetch` + reconcile | **VersionCon: Sync**                      | (no alias)                     | `versioncon.sync`                 | Resolves the out-of-sync tracker after teammate pushes |

   ### Differences from git you should know
   - VersionCon does NOT commit-as-you-go — changes are atomic pushes with messages, same as git, but the branch is the single source of truth and the workspace is a scratch pad. Re-pulling restores the branch state.
   - Conflicts are file-level (Phase 4) and will be function-level in Phase 5 (dependency-aware). There is no three-way merge to resolve manually in v1.
   - Cloud export is one-way per command — you can re-run **Export to Git Remote** any time, but each export is a fresh commit on top of the remote branch. No history reconciliation in v1.
   ```

3. After insertion, run `grep -c "Lifecycle Tour" README.md` to confirm exactly 1 occurrence; `grep -c "VersionCon for Git Users" README.md` ≥ 1.

DO NOT remove any existing README content. DO NOT add screenshots/GIFs (out of scope; deferred). DO NOT cross-link to phases inside the planning/ tree from README — README is user-facing and should not expose .planning/.
  </action>
  <verify>
    <automated>grep -c "Lifecycle Tour" README.md &amp;&amp; grep -c "VersionCon for Git Users" README.md &amp;&amp; grep -c "versioncon.exportToGitRemote" README.md</automated>
  </verify>
  <done>README.md contains both new sections; existing content preserved; command-id table accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror Lifecycle Tour + Git-user mapping into .planning/PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <action>
1. Read the current `.planning/PROJECT.md`. The file has top-level sections: What This Is / Core Value / Requirements / Out of Scope / Context / Constraints / Key Decisions / Evolution.

2. Insert a new top-level section `## Lifecycle Tour` AFTER the `## Constraints` section and BEFORE `## Key Decisions`. This places the lifecycle context in a planning-natural location — between the constraints that shape the design and the decisions that record what was chosen. Content mirrors the README's Lifecycle Tour but framed in planning-doc language (no "Alice and Bob" anecdote; instead a brief functional description of each stage):

   ```markdown
   ## Lifecycle Tour

   VersionCon v1 covers the full lifecycle from "empty folder" to "shipped to GitHub and starting v2." Each stage uses commands surfaced by phases shipped to date:

   1. **Create.** Host runs `versioncon.hostSession` (Phase 1). Wizard collects displayName + invite code (Phase 4.1). `.versioncon/branches/main/` initialized (Phase 3). `.versioncon/` hidden from File Explorer (Phase 4.3).
   2. **Join.** Teammates run `versioncon.joinSession` (Phase 1). Sidebar + member presence wire up (Phase 4).
   3. **Edit + Push.** Workspace-diff push (Phase 4.3 — `versioncon.push` auto-stages when nothing drag-staged). Status bar shows `N local changes` (Phase 4.3). Confirmation modal + per-file diff preview (Phase 3). Push broadcasts via SessionHost (Phase 4).
   4. **Pull.** `versioncon.pull` copies branch → workspace with PUSH-11 conflict prompt (Phase 4.3 reusing Phase 3 modal).
   5. **Sync after teammate push.** `versioncon.sync` resolves the SyncTracker out-of-sync set (Phase 3).
   6. **Ship to cloud.** Host runs `versioncon.exportToGitRemote` (Phase 4.3) — git init/add/commit/remote/push via spawn-with-shell:false. Admin-gated. Output to `VersionCon: Git Bridge` channel.
   7. **Start v2.** Host runs `versioncon.importFromGitRemote` (Phase 4.3) — clones a real Git remote into a fresh VersionCon branch. BranchManager.registerExternalBranch records metadata.

   ## VersionCon-for-Git-Users vocabulary

   Phase 4.3 introduced git-style command aliases so users coming from git have a familiar entry surface without duplicating logic. The aliases (`versioncon.cmd.<verb>`) are pure pass-throughs to canonical command ids — adding behavior to any verb MUST happen on the canonical id, never the alias module (see `src/commands/aliases.ts` JSDoc).

   | Git verb | Canonical command | Alias | Notes |
   |----------|-------------------|-------|-------|
   | push     | versioncon.push                  | cmd.push     | Workspace-diff auto-stage when un-staged |
   | pull     | versioncon.pull                  | cmd.pull     | New in Phase 4.3 — distinct from versioncon.sync |
   | checkout | versioncon.switchBranch          | cmd.checkout | |
   | branch   | versioncon.createBranch          | cmd.branch   | |
   | log      | versioncon.showPushHistory       | cmd.log      | |
   | diff     | versioncon.diff                  | cmd.diff     | Workspace-wide; versioncon.previewDiff is per-file |
   | merge    | versioncon.mergeBranch           | cmd.merge    | |
   | export   | versioncon.exportToGitRemote     | (none)       | Host-only, admin-gated |
   | import   | versioncon.importFromGitRemote   | (none)       | Host-only, admin-gated |
   | sync     | versioncon.sync                  | (none)       | Existing PUSH-10/11 sync resolution |
   ```

3. Add a row to the `## Key Decisions` table immediately after the existing `| Branch as source of truth, workspace as scratch | ... |` row:

   ```markdown
   | Git-style command aliases (Phase 4.3) | User feedback during Phase 4 UAT 2026-05-11 — git vocabulary lowers onboarding friction without changing the underlying model | — Pending validation |
   | Workspace-diff push/pull (Phase 4.3) | Drag-and-drop split-pane felt mandatory; workspace-diff lets users push from the native File Explorer | — Pending validation |
   | Cloud git bridge (Phase 4.3) | Phase 4 UAT: "Alice will push final branch to cloud or wherever they want to use the code." Closes the LAN-to-cloud lifecycle loop. | — Pending validation |
   ```

4. Verify with `grep -c "Lifecycle Tour" .planning/PROJECT.md` ≥ 1 and `grep -c "VersionCon-for-Git-Users vocabulary" .planning/PROJECT.md` ≥ 1.

DO NOT alter the `## Evolution` section — its update rules belong to /gsd-transition, not to a phase plan. DO NOT touch `## Out of Scope`. The Lifecycle Tour is meant to be a stable navigation aid, not a changelog.
  </action>
  <verify>
    <automated>grep -c "Lifecycle Tour" .planning/PROJECT.md &amp;&amp; grep -c "VersionCon-for-Git-Users vocabulary" .planning/PROJECT.md &amp;&amp; grep -c "Git-style command aliases (Phase 4.3)" .planning/PROJECT.md</automated>
  </verify>
  <done>PROJECT.md contains the new Lifecycle Tour + git-vocabulary table + 3 new rows in the Key Decisions table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Documentation-only plan; no code or test changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-DOC-01 | Information disclosure | README documents the existence of the cloud bridge, which could draw attacker attention to the export flow | accept | Threat already exists via the Command Palette UI; documentation only describes intent. The export flow itself is gated (T-04.3-04). |
| T-04.3-DOC-02 | Tampering | Outdated docs may mislead future planners | mitigate | Lifecycle Tour references phase numbers so a reader can find the current implementation. Future phase planners are required to update Key Decisions via /gsd-transition. |
</threat_model>

<test_plan>
- No new tests in this plan (documentation only)
- Verification via `grep -c` invariants on the two updated files
- After plan completes, total test count unchanged from end of Wave 4 (~428)
- Manual smoke: open README.md in VS Code preview — Lifecycle Tour renders; PROJECT.md preview shows the new sections
</test_plan>

<deviation_protocol>
Follow `$HOME/.claude/get-shit-done/references/deviation-protocol.md`. If the README does not yet exist (currently a stub), Rule 2: create it with the minimum structure described in Task 1, plus a one-line elevator pitch lifted from PROJECT.md's "Core Value" section so the new sections have context. If any verb mapping in the table drifts from the actual command id shipped by Waves 1-4 (e.g. executor renamed a command), update the table to match — the table is the contract, the code is the source of truth.
</deviation_protocol>

<verification>
- `grep -c "Lifecycle Tour" README.md` ≥ 1
- `grep -c "VersionCon for Git Users" README.md` ≥ 1
- `grep -c "Lifecycle Tour" .planning/PROJECT.md` ≥ 1
- `grep -c "VersionCon-for-Git-Users vocabulary" .planning/PROJECT.md` ≥ 1
- `grep -c "Git-style command aliases (Phase 4.3)" .planning/PROJECT.md` ≥ 1
- `npm test` passes; total test count unchanged from end of Wave 4
- Visual smoke: VS Code Markdown preview renders both files without syntax errors
</verification>

<success_criteria>
- README.md tells the user the full v1 lifecycle in plain prose + a translation table
- PROJECT.md absorbs the new vocabulary so future phase planning inherits it
- Key Decisions table updated with three Phase 4.3 rows
- No code or test files touched — pure documentation
- Phase 4.3 is complete after this plan ships
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-git-style-commands-file-explorer-cloud-bridge/04.3-05-SUMMARY.md`.
</output>
